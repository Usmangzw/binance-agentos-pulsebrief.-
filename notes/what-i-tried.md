Tried wrapping python-binance first. Fine for a bot, pointless here because Agent OS already gives market data through MCP.

Tried asking for 1m candles over 2 days. Responses got huge and the model started summarizing instead of calculating. 1h / 60 candles is enough for a brief.

Forgot to pin the interval once and got mixed 15m + 1h numbers. Prompt now states the interval twice.
